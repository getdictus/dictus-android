---
status: diagnosed
phase: 10-beta-distribution-license-audit
source: [10-01-SUMMARY.md, 10-02-SUMMARY.md, 10-03-SUMMARY.md]
started: 2026-04-01T13:00:00Z
updated: 2026-04-01T13:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Gradle Build Without Signing Credentials
expected: Run `./gradlew assembleDebug` locally without signing env vars. Build succeeds, debug APK produced. No NPE from missing keystore path.
result: pass

### 2. Licensee Audit Passes
expected: Run `./gradlew :app:licenseeDebug`. Task passes with no GPL violations. You may see 2 harmless warnings about unused CC-BY-4.0 and BSD-2-Clause allowlist entries.
result: pass

### 3. LicencesScreen Displays Auto-Generated + Manual Entries
expected: After running `./gradlew :app:licenseeDebug`, build and launch the app. Go to Settings > Licences. Screen shows Maven dependencies first (auto-generated from artifacts.json), then a "Vendored & Non-Maven" section header, then 4 manual entries (whisper.cpp, sherpa-onnx, NVIDIA Parakeet, Dictus).
result: issue
reported: "Only 4 manual entries visible, no Maven dependencies shown. artifacts.json generated by licensee in build/reports/ but not copied into APK assets — code reads from context.assets.open() but file is never bundled."
severity: major

### 4. LicencesScreen Fallback Without artifacts.json
expected: Run `./gradlew assembleDebug` WITHOUT running licenseeDebug first (clean build). Launch app, go to Settings > Licences. Screen shows only the 4 manual entries (no crash, no empty state error). Maven section is simply absent.
result: pass

### 5. README Content
expected: Open README.md at project root. Contains: project description, feature list, "Beta Installation" section with step-by-step sideloading instructions, download link to GitHub Releases, feedback link to GitHub Issues, tech stack section, MIT license notice.
result: pass

### 6. CI Workflow on Push
expected: Push to main (or open a PR). GitHub Actions runs the CI workflow with two parallel jobs: lint and test. Both jobs pass green. The test job runs licenseeDebug + assembleDebug and uploads a debug APK artifact.
result: pass

### 7. Release Workflow on Tag
expected: After configuring 4 GitHub Secrets (SIGNING_KEYSTORE, SIGNING_KEY_ALIAS, SIGNING_KEY_PASSWORD, SIGNING_STORE_PASSWORD), push a v* tag (e.g., v1.0.0). Release workflow decodes keystore, extracts version from tag, runs licenseeRelease + assembleRelease, and uploads a signed APK to GitHub Releases.
result: pass

## Summary

total: 7
passed: 6
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "LicencesScreen shows auto-generated Maven dependencies from artifacts.json above manual entries"
  status: failed
  reason: "artifacts.json generated by licensee in build/reports/licensee/androidDebug/ but never copied into APK assets. Code reads context.assets.open('app/cash/licensee/artifacts.json') which always fails silently, falling back to empty list."
  severity: major
  test: 3
  root_cause: "licensee generates artifacts.json in build/reports/licensee/androidDebug/ but no Gradle task copies it into APK assets. The code reads from context.assets.open() which only sees files in src/main/assets/ or merged asset directories."
  artifacts:
    - path: "app/build.gradle.kts"
      issue: "Missing Gradle task to copy licensee output into assets"
    - path: "app/src/main/java/dev/pivisolutions/dictus/ui/settings/LicencesScreen.kt"
      issue: "Asset path 'app/cash/licensee/artifacts.json' may need adjustment depending on copy destination"
  missing:
    - "Add Gradle task that copies build/reports/licensee/android{variant}/artifacts.json into merged assets before assemble"
  debug_session: ""
