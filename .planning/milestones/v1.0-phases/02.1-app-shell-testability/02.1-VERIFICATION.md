---
phase: 02.1-app-shell-testability
verified: 2026-03-24T23:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
human_verification:
  - test: "Launch the app on a physical device and confirm the test surface renders and recording flow works end to end"
    expected: "IME status card shows keyboard state, tapping Test Recording requests mic permission, waveform animates during recording, Stop/Cancel return to idle, permission denial shows Snackbar with Settings button"
    why_human: "SUMMARY.md records Pixel 4 approval from Task 3 (human checkpoint), but this verification cannot confirm the physical device test occurred. Visual rendering, waveform animation, timer counting, and Snackbar behavior cannot be verified programmatically."
---

# Phase 02.1: App Shell + Testability Verification Report

**Phase Goal:** Minimal app activity with IME status card, recording test surface, mic permission handling, and Compose UI test infrastructure across all modules
**Verified:** 2026-03-24
**Status:** human_needed (all automated checks pass; physical device test claimed but unconfirmable programmatically)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User opens the app and sees an IME status card showing whether Dictus keyboard is enabled/selected | VERIFIED | `ImeStatusCard.kt` implements 3 states (not enabled, enabled-not-selected, active) with correct text and action buttons; 3 Compose UI tests pass under Robolectric |
| 2 | User taps Test Recording, grants mic permission, and sees real-time waveform + timer | VERIFIED | `RecordingTestArea.kt` renders `WaveformBars` from core and formats timer as `%02d:%02d`; `TestSurfaceScreen.kt` wires permission launcher to `controller?.startRecording()`; 6 Compose UI tests confirm states |
| 3 | Permission denial shows Snackbar with Settings action | VERIFIED | `TestSurfaceScreen.kt` shows `snackbarHostState.showSnackbar(message="Microphone permission is required...", actionLabel="Settings")` on denied result, calls `onOpenAppSettings()` when action performed |
| 4 | FakeDictationController provides controllable test doubles for both app and ime module tests | VERIFIED | `core/src/test/.../FakeDictationController.kt` implements `DictationController` interface with `emitState()`, call counters; 5 unit tests in `FakeDictationControllerTest.kt` pass |
| 5 | ./gradlew testAll runs all unit tests across all 3 modules | VERIFIED | Root `build.gradle.kts` registers `testAll` task depending on `:app:testDebugUnitTest`, `:core:testDebugUnitTest`, `:ime:testDebugUnitTest` |

**Score:** 5/5 truths verified

---

### Required Artifacts

#### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/src/test/.../FakeDictationController.kt` | Shared test double implementing DictationController | VERIFIED | Contains `class FakeDictationController : DictationController`, `fun emitState(state: DictationState)`, call counters for all 3 methods |
| `core/src/test/.../FakeDictationControllerTest.kt` | Unit tests for FakeDictationController | VERIFIED | Contains `class FakeDictationControllerTest` with 5 tests covering initial state, start/stop/cancel transitions, and emitState |
| `core/src/main/.../core/ui/WaveformBars.kt` | Extracted WaveformBars composable shared between app and ime | VERIFIED | Contains `fun WaveformBars(`, `internal fun padEnergy(`, `internal fun barColor(`; package is `dev.pivisolutions.dictus.core.ui` |
| `build.gradle.kts` | Root testAll Gradle task | VERIFIED | Contains `tasks.register("testAll")` with `:app:testDebugUnitTest`, `:core:testDebugUnitTest`, `:ime:testDebugUnitTest` dependencies |

#### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/main/.../MainActivity.kt` | Activity with service binding and DictusTheme hosting TestSurfaceScreen | VERIFIED | `@AndroidEntryPoint class MainActivity : ComponentActivity()`, `DictationService.LocalBinder`, `ServiceConnection`, `bindDictationService()`, `checkImeStatus()`, `TestSurfaceScreen(` call |
| `app/src/main/.../ui/TestSurfaceScreen.kt` | Main screen composable combining ImeStatusCard and RecordingTestArea | VERIFIED | `fun TestSurfaceScreen(controller: DictationController?, ...)`, `Scaffold` with `SnackbarHost`, both child composables called, permission flow wired |
| `app/src/main/.../ui/ImeStatusCard.kt` | Card composable showing IME enabled/selected status | VERIFIED | `fun ImeStatusCard(isEnabled: Boolean, isSelected: Boolean, ...)`, 3-state `when` block with correct text and button labels |
| `app/src/main/.../ui/RecordingTestArea.kt` | Recording test area with waveform, timer, state display | VERIFIED | `fun RecordingTestArea(dictationState: DictationState, ...)`, imports `dev.pivisolutions.dictus.core.ui.WaveformBars`, `DictationState.Recording` branch with timer, waveform, Stop/Cancel buttons |
| `app/src/main/.../permission/PermissionHelper.kt` | Permission request composable wrapping RECORD_AUDIO + POST_NOTIFICATIONS | VERIFIED | `fun buildPermissionsToRequest()` with `TIRAMISU` guard, `fun rememberPermissionState(...)` composable using `rememberLauncherForActivityResult` |
| `app/src/test/.../ui/ImeStatusCardTest.kt` | Compose UI tests for IME status card rendering | VERIFIED | `@RunWith(RobolectricTestRunner::class) class ImeStatusCardTest` with 3 tests for all 3 states |
| `app/src/test/.../ui/RecordingTestAreaTest.kt` | Compose UI tests for recording test area states | VERIFIED | `@RunWith(RobolectricTestRunner::class) class RecordingTestAreaTest` with 6 tests covering idle, recording, timer, and button callbacks |
| `app/src/test/.../permission/PermissionHelperTest.kt` | Tests for permission list by API level | VERIFIED (implied) | `@RunWith(RobolectricTestRunner::class) class PermissionHelperTest` with `@Config(sdk=[30])` and `@Config(sdk=[33])` tests |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `gradle/libs.versions.toml` | `*/build.gradle.kts` | version catalog references | WIRED | `compose-ui-test-junit4`, `robolectric`, `coroutines-test` entries exist in catalog; all 3 modules reference `libs.compose.ui.test.junit4`, `libs.robolectric`, `libs.coroutines.test` in `testImplementation` |
| `core/src/test/.../FakeDictationController.kt` | `core/src/main/.../DictationController.kt` | implements interface | WIRED | `class FakeDictationController : DictationController` confirmed at line 14 |
| `app/src/main/.../MainActivity.kt` | `app/src/main/.../service/DictationService.kt` | ServiceConnection binding | WIRED | `(binder as? DictationService.LocalBinder)?.getService()` at line 64 of MainActivity |
| `app/src/main/.../ui/RecordingTestArea.kt` | `core/src/main/.../core/ui/WaveformBars.kt` | Composable import | WIRED | `import dev.pivisolutions.dictus.core.ui.WaveformBars` at line 23; `WaveformBars(energyLevels = dictationState.energy, ...)` called in Recording branch |
| `app/src/main/.../ui/TestSurfaceScreen.kt` | `core/src/main/.../core/service/DictationController.kt` | controller state observation | WIRED | `controller: DictationController?` parameter, `controller?.state?.collectAsState()?.value` drives `RecordingTestArea` |
| `ime/src/main/.../ime/ui/WaveformBars.kt` | `core/src/main/.../core/ui/WaveformBars.kt` | re-export wrapper | WIRED | `import dev.pivisolutions.dictus.core.ui.WaveformBars as CoreWaveformBars`; thin `@Composable fun WaveformBars(...)` delegates to `CoreWaveformBars` |

---

### Requirements Coverage

The plans for phase 02.1 declare requirement IDs SHELL-01 through SHELL-05. These IDs do **not appear in REQUIREMENTS.md** — they are not listed in the traceability table, and no `SHELL-*` requirements are defined anywhere in `.planning/REQUIREMENTS.md`.

| Requirement | Source Plan | Status | Notes |
|-------------|-------------|--------|-------|
| SHELL-01 | 02.1-02-PLAN.md | NOT IN REQUIREMENTS.MD | Functionally satisfied by ImeStatusCard + MainActivity IME status detection, but the ID is undeclared in REQUIREMENTS.md |
| SHELL-02 | 02.1-02-PLAN.md | NOT IN REQUIREMENTS.MD | Functionally satisfied by RecordingTestArea + TestSurfaceScreen permission flow, but ID undeclared |
| SHELL-03 | 02.1-02-PLAN.md | NOT IN REQUIREMENTS.MD | Functionally satisfied by Snackbar denial handling in TestSurfaceScreen, but ID undeclared |
| SHELL-04 | 02.1-01-PLAN.md | NOT IN REQUIREMENTS.MD | Functionally satisfied by FakeDictationController test double, but ID undeclared |
| SHELL-05 | 02.1-01-PLAN.md | NOT IN REQUIREMENTS.MD | Functionally satisfied by testAll Gradle task, but ID undeclared |

**Finding:** All SHELL-* requirement IDs referenced in both plans are orphaned — they exist only in plan frontmatter and the ROADMAP.md phase section, not in the canonical REQUIREMENTS.md. The functional work is delivered, but REQUIREMENTS.md was not updated to include these requirements. This is a documentation gap, not a functional gap. The ROADMAP.md phase section declares `Requirements: SHELL-01, SHELL-02, SHELL-03, SHELL-04, SHELL-05` but REQUIREMENTS.md has no matching entries.

---

### Anti-Patterns Found

No anti-patterns detected across phase files.

| File | Pattern | Severity | Result |
|------|---------|----------|--------|
| All UI composables | TODO/FIXME/placeholder | Scanned | None found |
| All UI composables | Empty return / stub body | Scanned | None found |
| Test files | Skipped/disabled tests | Scanned | None found |

---

### Human Verification Required

#### 1. End-to-End Recording Flow on Physical Device

**Test:** Build and install with `./gradlew installDebug`. Open Dictus app. Observe IME status card state. Tap "Test Recording", grant mic permission, verify waveform animates and timer counts up. Tap Stop, verify return to idle. Tap Test Recording, tap Cancel, verify return to idle. Clear app data, reopen, deny mic permission, verify Snackbar with Settings button appears.

**Expected:** All 12 verification steps from Plan 02 Task 3 pass as documented.

**Why human:** Visual rendering, waveform animation smoothness, timer tick accuracy, Snackbar appearance and dismiss behavior, and permission dialog interaction cannot be verified programmatically. SUMMARY.md documents Pixel 4 approval, but this verifier cannot confirm the physical test occurred.

---

### Commit Verification

All four task commits documented in SUMMARY files are confirmed in git history:

| Commit | Task | Status |
|--------|------|--------|
| `c1e9776` | Test dependencies and Robolectric config | VERIFIED in git log |
| `50c13e3` | FakeDictationController, WaveformBars extraction, testAll task | VERIFIED in git log |
| `6364c10` | UI composables with TDD tests | VERIFIED in git log |
| `ae59cb9` | MainActivity rewrite | VERIFIED in git log |

---

### Documentation Gap: SHELL-* IDs Not in REQUIREMENTS.md

REQUIREMENTS.md (last updated 2026-03-21) predates phase 02.1 (inserted into roadmap later). The SHELL-01 through SHELL-05 requirement IDs were created when phase 02.1 was inserted but REQUIREMENTS.md was not updated. The traceability table in REQUIREMENTS.md contains no entries for phase 02.1.

This does not block goal achievement — the functional deliverables are all present and verified. However, REQUIREMENTS.md should be updated to add the SHELL-* entries and their phase mapping for traceability completeness.

---

## Summary

Phase 02.1 goal is achieved. All 5 observable truths from the ROADMAP success criteria are verified against the actual codebase:

1. All artifacts exist and are substantive (no stubs or placeholder implementations)
2. All key links are wired (imports, interface implementations, service bindings, Gradle task dependencies)
3. Compose UI test infrastructure is active in all 3 modules (robolectric.properties, `isIncludeAndroidResources = true`, compose-ui-test-junit4, robolectric, coroutines-test)
4. 4 git commits are confirmed in history matching SUMMARY documentation
5. No anti-patterns detected

The only item requiring human confirmation is the physical device test (Pixel 4 runtime behavior), which SUMMARY.md claims was approved but cannot be confirmed programmatically.

The SHELL-* requirement IDs are a documentation gap: they appear in ROADMAP.md and plan frontmatter but not in REQUIREMENTS.md. This is a traceability maintenance issue, not a functional gap.

---

_Verified: 2026-03-24_
_Verifier: Claude (gsd-verifier)_
