---
phase: 02.1
slug: app-shell-testability
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-24
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 4.13.2 + Robolectric 4.14.1 + compose-ui-test-junit4 (via Compose BOM) |
| **Config file** | `{module}/src/test/resources/robolectric.properties` (Wave 0 installs) |
| **Quick run command** | `./gradlew testAll` |
| **Full suite command** | `./gradlew testAll` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `./gradlew testAll`
- **After every plan wave:** Run `./gradlew testAll`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| SHELL-01 | 01 | 1 | IME status card | unit (Compose) | `./gradlew :app:testDebugUnitTest --tests "*ImeStatusCardTest*"` | ❌ W0 | ⬜ pending |
| SHELL-02 | 01 | 1 | Recording test area | unit (Compose) | `./gradlew :app:testDebugUnitTest --tests "*RecordingTestAreaTest*"` | ❌ W0 | ⬜ pending |
| SHELL-03 | 01 | 1 | Permission request | unit | `./gradlew :app:testDebugUnitTest --tests "*PermissionHelperTest*"` | ❌ W0 | ⬜ pending |
| SHELL-04 | 01 | 1 | FakeDictationController | unit | `./gradlew :core:testDebugUnitTest --tests "*FakeDictationControllerTest*"` | ❌ W0 | ⬜ pending |
| SHELL-05 | 01 | 1 | testAll Gradle task | manual | `./gradlew testAll` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/src/test/resources/robolectric.properties` — Robolectric SDK config (`sdk=35`)
- [ ] `core/src/test/resources/robolectric.properties` — Robolectric SDK config
- [ ] `ime/src/test/resources/robolectric.properties` — Robolectric SDK config
- [ ] Add compose-ui-test-junit4, robolectric, coroutines-test, androidx-test dependencies to `gradle/libs.versions.toml`
- [ ] Add `testOptions.unitTests.isIncludeAndroidResources = true` to each module's `build.gradle.kts`
- [ ] Add `debugImplementation(libs.compose.ui.test.manifest)` to modules with Compose tests
- [ ] `core/src/test/.../FakeDictationController.kt` — shared test double
- [ ] Root `build.gradle.kts` `testAll` task

*Existing infrastructure: 8 JUnit tests across 3 modules. Wave 0 adds Compose UI test support and Robolectric.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| IME status reflects actual system state | SHELL-01 | Requires real InputMethodManager | 1. Open app 2. Check status card says "Not enabled" 3. Enable Dictus in Settings 4. Return to app 5. Verify status updates |
| Snackbar appears on permission denial | SHELL-03 | Permission dialog is system UI | 1. Tap "Test Recording" 2. Deny permission 3. Verify Snackbar with "Settings" action appears |
| Service binding works end-to-end | SHELL-02 | Requires real DictationService | 1. Open app 2. Grant mic permission 3. Tap "Test Recording" 4. Verify waveform displays 5. Tap stop 6. Verify returns to idle |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
