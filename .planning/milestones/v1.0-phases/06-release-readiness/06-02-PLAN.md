---
phase: 06-release-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/pivisolutions/dictus/ui/settings/LanguagePickerTest.kt
  - ime/src/main/res/values/strings.xml
  - ime/src/main/res/values-fr/strings.xml
  - ime/src/main/java/dev/pivisolutions/dictus/ime/ui/RecordingScreen.kt
  - ime/src/main/java/dev/pivisolutions/dictus/ime/ui/TranscribingScreen.kt
  - ime/src/main/java/dev/pivisolutions/dictus/ime/ui/EmojiPickerScreen.kt
  - core/src/main/res/values/strings.xml
  - core/src/main/res/values-fr/strings.xml
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/resources.properties
  - app/src/main/java/dev/pivisolutions/dictus/MainActivity.kt
autonomous: true
requirements:
  - DSG-02
  - DSG-03

must_haves:
  truths:
    - "IME screens use stringResource() for all user-visible text"
    - "core/ has French overlay for ime_name and subtype labels"
    - "MainActivity extends AppCompatActivity for locale API support"
    - "generateLocaleConfig is enabled in app build.gradle.kts"
    - "AppLocalesMetadataHolderService is declared in AndroidManifest.xml for API 24-32 locale persistence"
    - "LanguagePickerTest.kt exists with stub tests for setUiLanguage and getCurrentUiLanguage"
  artifacts:
    - path: "app/src/test/java/dev/pivisolutions/dictus/ui/settings/LanguagePickerTest.kt"
      provides: "Wave 0 test stubs for language picker logic"
      contains: "setUiLanguage"
    - path: "ime/src/main/res/values/strings.xml"
      provides: "English default strings for IME module"
      contains: "ime_recording_listening"
    - path: "ime/src/main/res/values-fr/strings.xml"
      provides: "French overlay strings for IME module"
      contains: "ime_recording_listening"
    - path: "core/src/main/res/values-fr/strings.xml"
      provides: "French overlay for shared strings (ime_name, subtypes)"
      contains: "Clavier Dictus"
    - path: "app/src/main/resources.properties"
      provides: "Unqualified locale declaration for generateLocaleConfig"
      contains: "unqualifiedResLocale=en-US"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "AppLocalesMetadataHolderService for API 24-32 locale persistence"
      contains: "AppLocalesMetadataHolderService"
  key_links:
    - from: "app/src/main/java/dev/pivisolutions/dictus/MainActivity.kt"
      to: "androidx.appcompat.app.AppCompatActivity"
      via: "class extends"
      pattern: "class MainActivity.*AppCompatActivity"
    - from: "app/build.gradle.kts"
      to: "generateLocaleConfig"
      via: "androidResources block"
      pattern: "generateLocaleConfig.*=.*true"
---

<objective>
Create Wave 0 test stubs for the language picker, extract IME module strings into resources, add French overlay for core/ strings, and set up the localization infrastructure (appcompat dependency, AppCompatActivity, generateLocaleConfig, manifest service).

Purpose: DSG-03 requires bilingual UI across all modules. DSG-02 is confirmed complete (existing theme system). This plan handles the ime/ and core/ string extraction, the Android infrastructure needed for in-app language switching, and the Wave 0 test scaffold required before plan 06-03 implements the language picker.
Output: LanguagePickerTest.kt stubs, ime/ and core/ string resources with FR overlay, MainActivity using AppCompatActivity, locale config auto-generation enabled.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-readiness/06-CONTEXT.md
@.planning/phases/06-release-readiness/06-RESEARCH.md

<interfaces>
<!-- From core/src/main/res/values/strings.xml (current state): -->
<!-- app_name, ime_name, subtype_azerty, subtype_qwerty, enable_ime_instructions -->

<!-- From MainActivity.kt line 56: -->
<!-- class MainActivity : ComponentActivity() — MUST change to AppCompatActivity -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create LanguagePickerTest.kt stub (Wave 0 — Nyquist requirement)</name>
  <files>
    app/src/test/java/dev/pivisolutions/dictus/ui/settings/LanguagePickerTest.kt
  </files>
  <action>
1. Create the directory `app/src/test/java/dev/pivisolutions/dictus/ui/settings/` if it does not exist.

2. Create `app/src/test/java/dev/pivisolutions/dictus/ui/settings/LanguagePickerTest.kt` with stub tests that will be filled in by plan 06-03. The stubs define the expected behavior contract for the language picker:

```kotlin
package dev.pivisolutions.dictus.ui.settings

import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner
import org.robolectric.annotation.Config

/**
 * Tests for in-app language picker logic.
 * Wave 0 stubs — plan 06-03 implements the production code and fills these in.
 */
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [33])
class LanguagePickerTest {

    @Test
    fun `setUiLanguage with fr sets French locale`() {
        // TODO: plan 06-03 implements — verify AppCompatDelegate.setApplicationLocales
        //       receives LocaleListCompat.forLanguageTags("fr")
    }

    @Test
    fun `setUiLanguage with en sets English locale`() {
        // TODO: plan 06-03 implements — verify AppCompatDelegate.setApplicationLocales
        //       receives LocaleListCompat.forLanguageTags("en")
    }

    @Test
    fun `setUiLanguage with system sets empty locale list`() {
        // TODO: plan 06-03 implements — verify AppCompatDelegate.setApplicationLocales
        //       receives LocaleListCompat.getEmptyLocaleList()
    }

    @Test
    fun `getCurrentUiLanguage returns system when no override set`() {
        // TODO: plan 06-03 implements — verify returns "system" when
        //       AppCompatDelegate.getApplicationLocales() is empty
    }

    @Test
    fun `getCurrentUiLanguage returns fr when French locale set`() {
        // TODO: plan 06-03 implements — verify returns "fr" when
        //       AppCompatDelegate.getApplicationLocales() returns fr locale
    }
}
```

Note: These are intentionally stub tests with empty bodies — they will pass immediately. Plan 06-03 Task 1 fills in the real assertions alongside the production code.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-android && ./gradlew :app:testDebugUnitTest --tests "*.LanguagePickerTest" 2>&1 | tail -5</automated>
  </verify>
  <done>LanguagePickerTest.kt exists with 5 stub tests for setUiLanguage() and getCurrentUiLanguage(). All stubs pass (empty bodies).</done>
</task>

<task type="auto">
  <name>Task 1: Extract ime/ strings and add core/ French overlay</name>
  <files>
    ime/src/main/res/values/strings.xml,
    ime/src/main/res/values-fr/strings.xml,
    ime/src/main/java/dev/pivisolutions/dictus/ime/ui/RecordingScreen.kt,
    ime/src/main/java/dev/pivisolutions/dictus/ime/ui/TranscribingScreen.kt,
    ime/src/main/java/dev/pivisolutions/dictus/ime/ui/EmojiPickerScreen.kt,
    core/src/main/res/values/strings.xml,
    core/src/main/res/values-fr/strings.xml
  </files>
  <read_first>
    ime/src/main/java/dev/pivisolutions/dictus/ime/ui/RecordingScreen.kt,
    ime/src/main/java/dev/pivisolutions/dictus/ime/ui/TranscribingScreen.kt,
    ime/src/main/java/dev/pivisolutions/dictus/ime/ui/EmojiPickerScreen.kt,
    core/src/main/res/values/strings.xml
  </read_first>
  <action>
1. Create `ime/src/main/res/values/` directory.

2. Create `ime/src/main/res/values/strings.xml` with English IME strings:
   ```xml
   <string name="ime_recording_listening">Listening…</string>
   <string name="ime_transcribing">Transcribing…</string>
   <string name="ime_emoji_return_keyboard">ABC</string>
   ```
   Read each IME screen file to discover ALL hardcoded strings — the list above is based on the research inventory but the actual files are authoritative.

3. Create `ime/src/main/res/values-fr/strings.xml` with the current French text:
   ```xml
   <string name="ime_recording_listening">En \u00e9coute\u2026</string>
   <string name="ime_transcribing">Transcription\u2026</string>
   <string name="ime_emoji_return_keyboard">ABC</string>
   ```
   Note: "ABC" is the same in both languages.

4. Update RecordingScreen.kt, TranscribingScreen.kt, EmojiPickerScreen.kt:
   - Add `import androidx.compose.ui.res.stringResource`
   - Replace hardcoded text with `stringResource(R.string.xxx)`
   - The R import for ime module is `import dev.pivisolutions.dictus.ime.R`

5. Create `core/src/main/res/values-fr/strings.xml` with French overlay for existing core strings:
   ```xml
   <?xml version="1.0" encoding="utf-8"?>
   <resources>
       <string name="ime_name">Clavier Dictus</string>
       <string name="subtype_azerty">AZERTY (Fran\u00e7ais)</string>
       <string name="subtype_qwerty">QWERTY (Anglais)</string>
   </resources>
   ```
   Do NOT duplicate `app_name` (Dictus is the same in both languages). Do NOT create a `values-en/` directory.

6. Update `core/src/main/res/values/strings.xml` — verify existing English strings are correct defaults. The current file already has English strings. No changes needed unless strings are in French.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-android && ./gradlew :ime:assembleDebug :core:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 IME screens use stringResource(). core/ has French overlay for ime_name and subtypes. Both modules compile successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Set up localization infrastructure (appcompat, AppCompatActivity, generateLocaleConfig, manifest service)</name>
  <files>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    app/src/main/AndroidManifest.xml,
    app/src/main/resources.properties,
    app/src/main/java/dev/pivisolutions/dictus/MainActivity.kt
  </files>
  <read_first>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    app/src/main/AndroidManifest.xml,
    app/src/main/java/dev/pivisolutions/dictus/MainActivity.kt
  </read_first>
  <action>
1. **Check and add appcompat dependency to version catalog:**
   Read `gradle/libs.versions.toml` first. Check if `appcompat` already exists in [versions] or [libraries] sections.
   - If appcompat is NOT present: In [versions] section, add: `appcompat = "1.7.0"`. In [libraries] section, add: `androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }`
   - If appcompat IS present but version < 1.6.0: Update the version to `1.7.0` (setApplicationLocales requires 1.6+)
   - If appcompat IS present at 1.6.0+: Leave unchanged, no action needed

2. **Add appcompat to app/build.gradle.kts:**
   Read `app/build.gradle.kts` first. Check if `libs.androidx.appcompat` already appears in dependencies.
   - If NOT present: In the dependencies block, add: `implementation(libs.androidx.appcompat)`
   - If already present: Leave unchanged

3. **Enable generateLocaleConfig in app/build.gradle.kts:**
   Inside the `android {}` block, add:
   ```kotlin
   androidResources {
       generateLocaleConfig = true
   }
   ```

4. **Create app/src/main/resources.properties:**
   ```properties
   unqualifiedResLocale=en-US
   ```
   This tells AGP that values/strings.xml is English, so the auto-generated LocaleConfig correctly declares `en` and `fr` as supported locales.

5. **Add AppLocalesMetadataHolderService to AndroidManifest.xml:**
   Inside the `<application>` tag in `app/src/main/AndroidManifest.xml`, add:
   ```xml
   <service
       android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
       android:enabled="false"
       android:exported="false">
       <meta-data
           android:name="autoStoreLocales"
           android:value="true" />
   </service>
   ```

6. **Change MainActivity to extend AppCompatActivity:**
   In `app/src/main/java/dev/pivisolutions/dictus/MainActivity.kt`:
   - Replace `import androidx.activity.ComponentActivity` with `import androidx.appcompat.app.AppCompatActivity`
   - Change `class MainActivity : ComponentActivity()` to `class MainActivity : AppCompatActivity()`
   - Keep all other code unchanged. AppCompatActivity extends FragmentActivity extends ComponentActivity, so all existing APIs (setContent, enableEdgeToEdge, etc.) remain available.
   - Note: `enableEdgeToEdge()` is from `androidx.activity.enableEdgeToEdge`, which works on AppCompatActivity since it extends ComponentActivity. Verify this import is retained.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-android && ./gradlew :app:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>Appcompat dependency added (or confirmed present), MainActivity extends AppCompatActivity, generateLocaleConfig enabled with resources.properties, AppLocalesMetadataHolderService declared in manifest. App compiles successfully.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug :ime:assembleDebug :core:assembleDebug` all compile
- `./gradlew :app:testDebugUnitTest --tests "*.LanguagePickerTest"` passes (stubs green)
- `./gradlew test` passes (no regressions)
- `grep -c "AppCompatActivity" app/src/main/java/dev/pivisolutions/dictus/MainActivity.kt` returns 1+
- `grep -c "generateLocaleConfig" app/build.gradle.kts` returns 1
- `grep -c "AppLocalesMetadataHolderService" app/src/main/AndroidManifest.xml` returns 1
- ime/ and core/ both have values-fr/strings.xml files
</verification>

<success_criteria>
- LanguagePickerTest.kt exists with stub tests (Nyquist Wave 0)
- IME screens use stringResource() for all user-visible text
- core/ has French overlay for shared strings
- Localization infrastructure complete: appcompat, AppCompatActivity, generateLocaleConfig, manifest service
- All modules compile and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-release-readiness/06-02-SUMMARY.md`
</output>
