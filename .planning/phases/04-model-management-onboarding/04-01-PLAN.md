---
phase: 04-model-management-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - core/src/main/res/font/dm_sans_extralight.ttf
  - core/src/main/res/font/dm_sans_regular.ttf
  - core/src/main/res/font/dm_sans_medium.ttf
  - core/src/main/res/font/dm_sans_semibold.ttf
  - core/src/main/res/font/dm_sans_bold.ttf
  - core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusTypography.kt
  - core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusColors.kt
  - core/src/main/java/dev/pivisolutions/dictus/core/ui/GlassCard.kt
  - core/src/main/java/dev/pivisolutions/dictus/core/ui/SkeletonCard.kt
  - core/src/main/java/dev/pivisolutions/dictus/core/preferences/PreferenceKeys.kt
  - app/src/main/java/dev/pivisolutions/dictus/model/ModelManager.kt
  - app/src/main/java/dev/pivisolutions/dictus/navigation/AppDestination.kt
  - app/src/test/java/dev/pivisolutions/dictus/model/ModelCatalogTest.kt
  - app/src/test/java/dev/pivisolutions/dictus/model/ModelManagerTest.kt
autonomous: true
requirements: [APP-02, APP-03, APP-04]

must_haves:
  truths:
    - "ModelCatalog contains exactly 4 Whisper models with correct sizes and provider field"
    - "ModelManager can check if a model is downloaded and calculate storage used"
    - "ModelManager prevents deleting the last remaining model"
    - "DM Sans font renders with 5 weight variants in DictusTypography"
    - "GlassCard composable renders with correct surface color, border, corner radius"
    - "PreferenceKeys has all settings keys for Phase 4"
    - "Navigation Compose dependency is available in app module"
  artifacts:
    - path: "app/src/main/java/dev/pivisolutions/dictus/model/ModelManager.kt"
      provides: "4-model catalog with AiProvider enum, canDelete guard, storageUsedBytes"
      contains: "enum class AiProvider"
    - path: "core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusTypography.kt"
      provides: "DM Sans font family with 5 weights"
      contains: "DmSans"
    - path: "core/src/main/java/dev/pivisolutions/dictus/core/ui/GlassCard.kt"
      provides: "Shared glass-morphism card composable"
      contains: "fun GlassCard"
    - path: "core/src/main/java/dev/pivisolutions/dictus/core/preferences/PreferenceKeys.kt"
      provides: "All DataStore keys for Phase 4 settings"
      contains: "HAS_COMPLETED_ONBOARDING"
    - path: "app/src/main/java/dev/pivisolutions/dictus/navigation/AppDestination.kt"
      provides: "Navigation route definitions"
      contains: "sealed class AppDestination"
  key_links:
    - from: "core/.../theme/DictusTypography.kt"
      to: "core/src/main/res/font/"
      via: "FontFamily resource references"
      pattern: "R\\.font\\.dm_sans"
    - from: "app/.../model/ModelManager.kt"
      to: "ModelCatalog.ALL"
      via: "Catalog listing"
      pattern: "ModelCatalog"
---

<objective>
Foundation layer for Phase 4: navigation dependency, DM Sans typography, GlassCard component, extended color tokens, PreferenceKeys, ModelManager refactor with provider abstraction and 4-model catalog.

Purpose: All downstream plans (onboarding screens, model management UI, settings, navigation) depend on these types, components, and infrastructure being in place first.
Output: Build-passing project with new dependencies, theme updates, shared UI components, model catalog with tests.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-management-onboarding/04-CONTEXT.md
@.planning/phases/04-model-management-onboarding/04-RESEARCH.md
@.planning/phases/04-model-management-onboarding/04-UI-SPEC.md

<interfaces>
<!-- Current ModelManager (to be refactored): -->
From app/.../model/ModelManager.kt:
```kotlin
class ModelManager(context: Context) {
    companion object {
        const val DEFAULT_MODEL_KEY = "tiny"
        val MODELS = mapOf(
            "tiny" to ModelInfo("ggml-tiny.bin", 77_691_713L),
            "small-q5_1" to ModelInfo("ggml-small-q5_1.bin", 190_031_232L),
        )
    }
    fun getModelPath(modelKey: String): String?
    fun downloadUrl(modelKey: String): String?
    fun getTargetPath(modelKey: String): String?
    data class ModelInfo(val fileName: String, val expectedSize: Long)
}
```

From core/.../preferences/PreferenceKeys.kt:
```kotlin
object PreferenceKeys {
    val KEYBOARD_LAYOUT = stringPreferencesKey("keyboard_layout")
    val ACTIVE_MODEL = stringPreferencesKey("active_model")
    val TRANSCRIPTION_LANGUAGE = stringPreferencesKey("transcription_language")
}
```

From core/.../theme/DictusColors.kt:
```kotlin
object DictusColors {
    val Background = Color(0xFF0A1628)
    val Accent = Color(0xFF3D7EFF)
    val AccentHighlight = Color(0xFF6BA3FF)
    val Surface = Color(0xFF161C2C)
    val Recording = Color(0xFFEF4444)
    val Success = Color(0xFF22C55E)
    // ... keyboard colors
}
```

From core/.../theme/DictusTypography.kt:
```kotlin
val DictusTypography = Typography(
    bodyLarge = TextStyle(fontFamily = FontFamily.Default, ...),
    titleLarge = TextStyle(fontFamily = FontFamily.Default, ...),
    labelSmall = TextStyle(fontFamily = FontFamily.Default, ...),
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependencies, DM Sans font, theme updates, GlassCard, PreferenceKeys, navigation routes</name>
  <files>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    core/src/main/res/font/dm_sans_extralight.ttf,
    core/src/main/res/font/dm_sans_regular.ttf,
    core/src/main/res/font/dm_sans_medium.ttf,
    core/src/main/res/font/dm_sans_semibold.ttf,
    core/src/main/res/font/dm_sans_bold.ttf,
    core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusTypography.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusColors.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/ui/GlassCard.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/ui/SkeletonCard.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/preferences/PreferenceKeys.kt,
    app/src/main/java/dev/pivisolutions/dictus/navigation/AppDestination.kt
  </files>
  <read_first>
    gradle/libs.versions.toml,
    app/build.gradle.kts,
    core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusTypography.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/theme/DictusColors.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/preferences/PreferenceKeys.kt,
    core/src/main/java/dev/pivisolutions/dictus/core/ui/WaveformBars.kt
  </read_first>
  <behavior>
    - DictusColors.AccentDark equals Color(0xFF2563EB)
    - DictusColors.SuccessDark equals Color(0xFF16A34A)
    - DictusColors.SuccessSubtle equals Color(0x3322C55E)
    - DictusColors.TextPrimary equals Color(0xFFFAFAF9)
    - DictusColors.TextSecondary equals Color(0xFF6B6B70)
    - DictusColors.BorderSubtle equals Color(0xFF2A2A2E)
    - DictusColors.GlassBorder equals Color(0x26FFFFFF) (white ~15% opacity)
    - DictusColors.InactiveDot equals Color(0x4DFFFFFF) (white ~30% opacity)
    - DictusColors.IconBackground equals Color(0xFF1E2A4A)
    - PreferenceKeys.HAS_COMPLETED_ONBOARDING is a booleanPreferencesKey
    - PreferenceKeys.HAPTICS_ENABLED is a booleanPreferencesKey
    - PreferenceKeys.SOUND_ENABLED is a booleanPreferencesKey
    - AppDestination has Onboarding, Main sealed variants
  </behavior>
  <action>
1. **libs.versions.toml** — Add navigation version and library:
   ```toml
   [versions]
   navigation = "2.8.9"

   [libraries]
   navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation" }
   okhttp-mockwebserver = { group = "com.squareup.okhttp3", name = "mockwebserver", version.ref = "okhttp" }
   ```

2. **app/build.gradle.kts** — Add dependencies:
   ```kotlin
   implementation(libs.navigation.compose)
   testImplementation(libs.okhttp.mockwebserver)
   ```

3. **DM Sans font files** — Download DM Sans from Google Fonts. Place 5 weight variants in `core/src/main/res/font/`:
   - `dm_sans_extralight.ttf` (Weight 200)
   - `dm_sans_regular.ttf` (Weight 400)
   - `dm_sans_medium.ttf` (Weight 500)
   - `dm_sans_semibold.ttf` (Weight 600)
   - `dm_sans_bold.ttf` (Weight 700)
   Download from: https://fonts.google.com/specimen/DM+Sans — download the family ZIP, extract the static .ttf files for these 5 weights, rename to snake_case. Filenames MUST be lowercase with underscores (Android resource naming).

4. **DictusTypography.kt** — Full rewrite with DM Sans:
   ```kotlin
   package dev.pivisolutions.dictus.core.theme

   import androidx.compose.material3.Typography
   import androidx.compose.ui.text.TextStyle
   import androidx.compose.ui.text.font.Font
   import androidx.compose.ui.text.font.FontFamily
   import androidx.compose.ui.text.font.FontWeight
   import androidx.compose.ui.unit.sp
   import dev.pivisolutions.dictus.core.R

   val DmSans = FontFamily(
       Font(R.font.dm_sans_extralight, FontWeight.ExtraLight),
       Font(R.font.dm_sans_regular, FontWeight.Normal),
       Font(R.font.dm_sans_medium, FontWeight.Medium),
       Font(R.font.dm_sans_semibold, FontWeight.SemiBold),
       Font(R.font.dm_sans_bold, FontWeight.Bold),
   )

   val DictusTypography = Typography(
       displayLarge = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Bold, fontSize = 32.sp, letterSpacing = (-0.5).sp),
       titleLarge = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.SemiBold, fontSize = 28.sp, letterSpacing = (-0.5).sp),
       titleMedium = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.SemiBold, fontSize = 17.sp),
       bodyLarge = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Normal, fontSize = 16.sp),
       bodyMedium = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Normal, fontSize = 15.sp, lineHeight = (15 * 1.5).sp),
       bodySmall = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Normal, fontSize = 13.sp),
       labelLarge = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Medium, fontSize = 14.sp),
       labelMedium = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Medium, fontSize = 13.sp),
       labelSmall = TextStyle(fontFamily = DmSans, fontWeight = FontWeight.Normal, fontSize = 12.sp),
   )
   ```

5. **DictusColors.kt** — Add missing color tokens per UI-SPEC:
   ```kotlin
   val AccentDark = Color(0xFF2563EB)
   val SuccessDark = Color(0xFF16A34A)
   val SuccessSubtle = Color(0x3322C55E)  // #22C55E at 20% opacity
   val TextPrimary = Color(0xFFFAFAF9)
   val TextSecondary = Color(0xFF6B6B70)
   val BorderSubtle = Color(0xFF2A2A2E)
   val GlassBorder = Color(0x26FFFFFF)    // white at ~15% opacity
   val InactiveDot = Color(0x4DFFFFFF)    // white at ~30% opacity
   val IconBackground = Color(0xFF1E2A4A)
   val Destructive = Color(0xFFEF4444)    // alias for Recording, semantic name
   ```
   Keep all existing colors unchanged.

6. **GlassCard.kt** — Create in `core/.../ui/`:
   ```kotlin
   @Composable
   fun GlassCard(
       modifier: Modifier = Modifier,
       content: @Composable ColumnScope.() -> Unit
   ) {
       Column(
           modifier = modifier
               .clip(RoundedCornerShape(16.dp))
               .background(DictusColors.Surface)
               .border(1.dp, DictusColors.GlassBorder, RoundedCornerShape(16.dp))
               .padding(20.dp),
           content = content
       )
   }
   ```

7. **SkeletonCard.kt** — Create shimmer loading placeholder:
   ```kotlin
   @Composable
   fun SkeletonCard(modifier: Modifier = Modifier) {
       // Animated shimmer using InfiniteTransition
       // Gradient sweep from #FFFFFF08 to #FFFFFF15, horizontal, 1200ms repeat
       // Wrapped in GlassCard shape (16dp corner, Surface background)
   }
   ```

8. **PreferenceKeys.kt** — Add new keys:
   ```kotlin
   val HAS_COMPLETED_ONBOARDING = booleanPreferencesKey("has_completed_onboarding")
   val HAPTICS_ENABLED = booleanPreferencesKey("haptics_enabled")
   val SOUND_ENABLED = booleanPreferencesKey("sound_enabled")
   ```
   Import `booleanPreferencesKey` from `androidx.datastore.preferences.core`.

9. **AppDestination.kt** — Create navigation routes:
   ```kotlin
   package dev.pivisolutions.dictus.navigation

   sealed class AppDestination(val route: String) {
       data object Onboarding : AppDestination("onboarding")
       data object Main : AppDestination("main")
       data object Home : AppDestination("home")
       data object Models : AppDestination("models")
       data object Settings : AppDestination("settings")
   }
   ```
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug :core:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - gradle/libs.versions.toml contains `navigation = "2.8.9"` and `navigation-compose`
    - app/build.gradle.kts contains `implementation(libs.navigation.compose)`
    - core/src/main/res/font/ contains 5 .ttf files matching dm_sans_*
    - DictusTypography.kt contains `val DmSans = FontFamily(`
    - DictusTypography.kt contains `R.font.dm_sans_extralight`
    - DictusColors.kt contains `val AccentDark = Color(0xFF2563EB)`
    - DictusColors.kt contains `val TextPrimary = Color(0xFFFAFAF9)`
    - DictusColors.kt contains `val TextSecondary = Color(0xFF6B6B70)`
    - DictusColors.kt contains `val BorderSubtle = Color(0xFF2A2A2E)`
    - DictusColors.kt contains `val GlassBorder = Color(0x26FFFFFF)`
    - GlassCard.kt contains `fun GlassCard(`
    - SkeletonCard.kt contains `fun SkeletonCard(`
    - PreferenceKeys.kt contains `val HAS_COMPLETED_ONBOARDING = booleanPreferencesKey`
    - PreferenceKeys.kt contains `val HAPTICS_ENABLED = booleanPreferencesKey`
    - PreferenceKeys.kt contains `val SOUND_ENABLED = booleanPreferencesKey`
    - AppDestination.kt contains `sealed class AppDestination`
    - AppDestination.kt contains `data object Onboarding`
    - `./gradlew :app:assembleDebug :core:assembleDebug` exits 0
  </acceptance_criteria>
  <done>All new dependencies compile, DM Sans font bundled with 5 weights, DictusTypography uses DmSans, DictusColors has all UI-SPEC tokens, GlassCard and SkeletonCard composables exist, PreferenceKeys extended, AppDestination routes defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelManager refactor with provider abstraction, 4-model catalog, and tests</name>
  <files>
    app/src/main/java/dev/pivisolutions/dictus/model/ModelManager.kt,
    app/src/test/java/dev/pivisolutions/dictus/model/ModelCatalogTest.kt,
    app/src/test/java/dev/pivisolutions/dictus/model/ModelManagerTest.kt
  </files>
  <read_first>
    app/src/main/java/dev/pivisolutions/dictus/model/ModelManager.kt,
    app/src/main/java/dev/pivisolutions/dictus/service/DictationService.kt,
    app/src/main/java/dev/pivisolutions/dictus/service/ModelDownloader.kt
  </read_first>
  <behavior>
    - ModelCatalog.ALL has exactly 4 entries: tiny, base, small, small-q5_1
    - ModelCatalog.ALL[0].key == "tiny" and expectedSizeBytes == 77_691_713L
    - ModelCatalog.ALL[1].key == "base" and expectedSizeBytes == 142_000_000L
    - ModelCatalog.ALL[2].key == "small" and expectedSizeBytes == 466_000_000L
    - ModelCatalog.ALL[3].key == "small-q5_1" and expectedSizeBytes == 190_031_232L
    - Every ModelInfo has provider == AiProvider.WHISPER
    - ModelCatalog.DEFAULT_KEY == "tiny"
    - ModelManager.canDelete() returns false when exactly 1 model is downloaded
    - ModelManager.canDelete() returns true when 2+ models are downloaded
    - ModelManager.storageUsedBytes() returns sum of sizes of downloaded models
    - ModelManager.storageUsedBytes() returns 0 when no models downloaded
    - ModelManager.deleteModel() returns true and deletes file when canDelete is true
    - ModelManager.deleteModel() returns false when canDelete is false (last model)
    - ModelManager.getDownloadedModels() returns list of keys for models present on disk
  </behavior>
  <action>
Refactor ModelManager.kt with provider abstraction. Keep backward compatibility for existing consumers (DictationService, ModelDownloader).

1. **Add AiProvider enum and ModelInfo data class** at top of file:
   ```kotlin
   enum class AiProvider { WHISPER, PARAKEET }

   data class ModelInfo(
       val key: String,
       val fileName: String,
       val displayName: String,
       val expectedSizeBytes: Long,
       val qualityLabel: String,
       val provider: AiProvider = AiProvider.WHISPER,
       val isDeprecated: Boolean = false,
   )
   ```

2. **Add ModelCatalog object** with 4 models:
   ```kotlin
   object ModelCatalog {
       const val DEFAULT_KEY = "tiny"
       private const val WHISPER_BASE_URL = "https://huggingface.co/ggerganov/whisper.cpp/resolve/main"

       val ALL = listOf(
           ModelInfo("tiny", "ggml-tiny.bin", "Tiny", 77_691_713L, "Rapide"),
           ModelInfo("base", "ggml-base.bin", "Base", 142_000_000L, "Equilibre"),
           ModelInfo("small", "ggml-small.bin", "Small", 466_000_000L, "Precis"),
           ModelInfo("small-q5_1", "ggml-small-q5_1.bin", "Small Q5", 190_031_232L, "Precis"),
       )

       fun findByKey(key: String): ModelInfo? = ALL.find { it.key == key }

       fun downloadUrl(info: ModelInfo): String = when (info.provider) {
           AiProvider.WHISPER -> "$WHISPER_BASE_URL/${info.fileName}"
           AiProvider.PARAKEET -> error("Parakeet not yet supported")
       }
   }
   ```

3. **Refactor ModelManager class** to use ModelCatalog:
   ```kotlin
   class ModelManager(context: Context) {
       companion object {
           const val DEFAULT_MODEL_KEY = ModelCatalog.DEFAULT_KEY
       }

       private val modelsDir = File(context.filesDir, "models").also { it.mkdirs() }

       fun getModelPath(modelKey: String): String? { /* same logic but use ModelCatalog.findByKey */ }
       fun downloadUrl(modelKey: String): String? { /* delegate to ModelCatalog.downloadUrl */ }
       fun getTargetPath(modelKey: String): String? { /* same logic but use ModelCatalog.findByKey */ }

       fun isDownloaded(modelKey: String): Boolean = getModelPath(modelKey) != null

       fun getDownloadedModels(): List<String> = ModelCatalog.ALL.filter { isDownloaded(it.key) }.map { it.key }

       fun canDelete(modelKey: String): Boolean {
           val downloaded = getDownloadedModels()
           return downloaded.size > 1 && modelKey in downloaded
       }

       fun deleteModel(modelKey: String): Boolean {
           if (!canDelete(modelKey)) return false
           val info = ModelCatalog.findByKey(modelKey) ?: return false
           return File(modelsDir, info.fileName).delete()
       }

       fun storageUsedBytes(): Long {
           return ModelCatalog.ALL.sumOf { info ->
               val file = File(modelsDir, info.fileName)
               if (file.exists()) file.length() else 0L
           }
       }

       fun modelFileSize(modelKey: String): Long {
           val info = ModelCatalog.findByKey(modelKey) ?: return 0L
           val file = File(modelsDir, info.fileName)
           return if (file.exists()) file.length() else 0L
       }
   }
   ```

4. **ModelCatalogTest.kt** — TDD tests:
   - Catalog has exactly 4 entries
   - All keys are unique
   - All have AiProvider.WHISPER
   - DEFAULT_KEY is "tiny"
   - findByKey returns correct ModelInfo
   - findByKey returns null for unknown key
   - downloadUrl returns HuggingFace URL containing fileName

5. **ModelManagerTest.kt** — TDD tests using temp directory:
   - getModelPath returns null when file does not exist
   - getModelPath returns path when file exists with correct size
   - getModelPath returns null when file has wrong size (corrupt)
   - isDownloaded returns false when no file
   - getDownloadedModels returns empty list when nothing downloaded
   - canDelete returns false when only 1 model downloaded
   - canDelete returns true when 2 models downloaded
   - deleteModel returns false when last model
   - deleteModel returns true and removes file when multiple models exist
   - storageUsedBytes returns 0 when empty
   - storageUsedBytes returns sum of actual file sizes
  </action>
  <verify>
    <automated>./gradlew :app:test --tests "*.ModelCatalogTest" --tests "*.ModelManagerTest" -x lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ModelManager.kt contains `enum class AiProvider`
    - ModelManager.kt contains `object ModelCatalog`
    - ModelManager.kt contains `val ALL = listOf(` with 4 ModelInfo entries
    - ModelManager.kt contains `fun canDelete(`
    - ModelManager.kt contains `fun deleteModel(`
    - ModelManager.kt contains `fun storageUsedBytes(`
    - ModelManager.kt contains `fun getDownloadedModels(`
    - ModelCatalogTest.kt exists and all tests pass
    - ModelManagerTest.kt exists and all tests pass
    - `./gradlew :app:test --tests "*.ModelCatalogTest" --tests "*.ModelManagerTest" -x lint` exits 0
  </acceptance_criteria>
  <done>ModelManager refactored with AiProvider enum, ModelCatalog with 4 models, canDelete guard, deleteModel, storageUsedBytes, getDownloadedModels. All tests green.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug :core:assembleDebug` builds successfully
- `./gradlew :app:test --tests "*.ModelCatalogTest" --tests "*.ModelManagerTest" -x lint` all pass
- No existing tests broken: `./gradlew :app:test :core:test -x lint`
</verification>

<success_criteria>
1. Navigation Compose dependency added and compiles
2. DM Sans font bundled with 5 weights, DictusTypography uses DmSans FontFamily
3. DictusColors extended with all UI-SPEC tokens (AccentDark, SuccessDark, TextPrimary, TextSecondary, BorderSubtle, GlassBorder, etc.)
4. GlassCard and SkeletonCard composables exist in core module
5. PreferenceKeys extended with HAS_COMPLETED_ONBOARDING, HAPTICS_ENABLED, SOUND_ENABLED
6. AppDestination sealed class with Onboarding, Main, Home, Models, Settings routes
7. ModelManager refactored with AiProvider enum, 4-model ModelCatalog, canDelete/deleteModel/storageUsedBytes
8. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-management-onboarding/04-01-SUMMARY.md`
</output>
