---
phase: 11-oss-repository
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - OSS-02

must_haves:
  truths:
    - "Filing a bug presents structured fields for device, STT provider, model, steps to reproduce, and app version"
    - "Filing a feature request presents use case and proposed solution fields"
    - "Opening a PR pre-fills a checklist with tests pass, no warnings, tested on device"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug report form"
      contains: "name: Bug report"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Feature request form"
      contains: "name: Feature request"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR checklist"
      contains: "Tests pass"
  key_links: []
---

<objective>
Create GitHub issue templates (YAML forms for bug report and feature request) and a PR template with a simple checklist.

Purpose: Contributors filing bugs provide actionable info (device, STT provider, steps); PRs have a consistent quality checklist.
Output: `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/PULL_REQUEST_TEMPLATE.md`
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-oss-repository/11-CONTEXT.md
@.planning/phases/11-oss-repository/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML issue templates</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml</files>
  <read_first>
    - .planning/phases/11-oss-repository/11-RESEARCH.md (contains complete YAML examples to use verbatim)
    - .planning/phases/11-oss-repository/11-CONTEXT.md (locked field decisions)
  </read_first>
  <action>
Create directory `.github/ISSUE_TEMPLATE/` (note: capital ISSUE_TEMPLATE) and two YAML files.

**`.github/ISSUE_TEMPLATE/bug_report.yml`:**
```yaml
name: Bug report
description: Something isn't working as expected
labels: ["bug"]
body:
  - type: input
    id: device
    attributes:
      label: Device and Android version
      placeholder: "e.g. Pixel 6, Android 13 (API 33)"
    validations:
      required: true
  - type: dropdown
    id: stt-provider
    attributes:
      label: STT provider
      options:
        - Whisper
        - Parakeet
    validations:
      required: true
  - type: input
    id: stt-model
    attributes:
      label: STT model
      placeholder: "e.g. Whisper tiny, Parakeet 110M INT8"
    validations:
      required: true
  - type: input
    id: app-version
    attributes:
      label: App version
      placeholder: "e.g. 1.1.0"
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      value: |
        1.
        2.
        3.
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
    validations:
      required: true
```

**`.github/ISSUE_TEMPLATE/feature_request.yml`:**
```yaml
name: Feature request
description: Suggest an improvement or new feature
labels: ["enhancement"]
body:
  - type: textarea
    id: use-case
    attributes:
      label: Use case
      description: What problem does this solve? What are you trying to do?
    validations:
      required: true
  - type: textarea
    id: proposed-solution
    attributes:
      label: Proposed solution
      description: How would you like this to work?
    validations:
      required: false
```

Important: Do NOT create a `config.yml` in ISSUE_TEMPLATE (per locked decision -- no external links or Discussions).
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && grep -q "name: Bug report" .github/ISSUE_TEMPLATE/bug_report.yml && grep -q "stt-provider" .github/ISSUE_TEMPLATE/bug_report.yml && grep -q "name: Feature request" .github/ISSUE_TEMPLATE/feature_request.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .github/ISSUE_TEMPLATE/bug_report.yml contains "name: Bug report"
    - .github/ISSUE_TEMPLATE/bug_report.yml contains "type: dropdown" with "stt-provider" id
    - .github/ISSUE_TEMPLATE/bug_report.yml contains options "Whisper" and "Parakeet"
    - .github/ISSUE_TEMPLATE/bug_report.yml contains "type: input" with "app-version" id
    - .github/ISSUE_TEMPLATE/bug_report.yml contains "type: textarea" with "steps" id
    - .github/ISSUE_TEMPLATE/bug_report.yml contains labels: ["bug"]
    - .github/ISSUE_TEMPLATE/feature_request.yml contains "name: Feature request"
    - .github/ISSUE_TEMPLATE/feature_request.yml contains "use-case" id
    - .github/ISSUE_TEMPLATE/feature_request.yml contains "proposed-solution" id
    - .github/ISSUE_TEMPLATE/feature_request.yml contains labels: ["enhancement"]
    - No config.yml exists in .github/ISSUE_TEMPLATE/
  </acceptance_criteria>
  <done>Bug report template collects device, STT provider (dropdown), model, app version, steps to reproduce, expected/actual behavior. Feature request template collects use case and proposed solution.</done>
</task>

<task type="auto">
  <name>Task 2: Create PR template</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/11-oss-repository/11-RESEARCH.md (PR template pattern)
    - .planning/phases/11-oss-repository/11-CONTEXT.md (locked: simple checklist, no dropdowns/screenshots sections)
  </read_first>
  <action>
Create `.github/PULL_REQUEST_TEMPLATE.md` with this exact content:

```markdown
## Description

<!-- Describe your changes -->

## Checklist

- [ ] Tests pass (`./gradlew test`)
- [ ] No new lint warnings (`./gradlew lint`)
- [ ] Tested on device (if UI change)
```

This is intentionally minimal per the locked decision -- no dropdown sections, no screenshot requirements, no elaborate categories.
  </action>
  <verify>
    <automated>test -f .github/PULL_REQUEST_TEMPLATE.md && grep -q "Tests pass" .github/PULL_REQUEST_TEMPLATE.md && grep -q "lint" .github/PULL_REQUEST_TEMPLATE.md && grep -q "Tested on device" .github/PULL_REQUEST_TEMPLATE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .github/PULL_REQUEST_TEMPLATE.md contains "## Description"
    - .github/PULL_REQUEST_TEMPLATE.md contains "## Checklist"
    - .github/PULL_REQUEST_TEMPLATE.md contains "Tests pass (`./gradlew test`)"
    - .github/PULL_REQUEST_TEMPLATE.md contains "No new lint warnings (`./gradlew lint`)"
    - .github/PULL_REQUEST_TEMPLATE.md contains "Tested on device (if UI change)"
  </acceptance_criteria>
  <done>PR template pre-fills description area and 3-item checklist covering tests, lint, and device testing.</done>
</task>

</tasks>

<verification>
All template files in correct locations:
- `ls .github/ISSUE_TEMPLATE/bug_report.yml .github/ISSUE_TEMPLATE/feature_request.yml .github/PULL_REQUEST_TEMPLATE.md` succeeds
- No `config.yml` in `.github/ISSUE_TEMPLATE/`
- Bug report has all required fields (device, stt-provider dropdown, stt-model, app-version, steps, expected, actual)
- Feature request has use-case and proposed-solution
- PR template has description + 3-item checklist
</verification>

<success_criteria>
- GitHub template chooser will show "Bug report" and "Feature request" when opening a new issue
- PR body is pre-filled with description section and checklist
- No config.yml or external links in templates
</success_criteria>

<output>
After completion, create `.planning/phases/11-oss-repository/11-02-SUMMARY.md`
</output>
